PR-SWEEP EC2 BUILD/VALIDATION QUEUE
Generated by the read+reply+fix sweep. No builds were run here (EC2 agent owns that).
Each item below is a branch that got a code change this sweep and needs a build to
confirm it compiles and its test passes on the OSv image. All branches are pushed to
gh-fork (github.com/gburd/osv-1) and rebased onto current upstream/master.

Build command shape (adjust per test):
  scripts/build image=native-example                # or the usual full build
  scripts/build check                               # runs the tst-* suite
  # for the ext4/rofs disk-backed tests, boot with the appropriate second disk.

---------------------------------------------------------------------------------
PR#1432  mm: implement mremap(2)                     branch pr/mremap  head 3f8152e
  Changes this sweep: __mremap symbol export (2 files), old_size==0 EINVAL guard,
  file-offset-mid-VMA fix in inspect_mremap_src, shrink-path munmap return check,
  MAP_PRIVATE-writable file-move now EINVAL, "ponytail"->TODO, <errno.h> in test.
  VALIDATE: compile (esp. e.to_libc() usage in core/mmu.cc; the __mremap symbol
  entries must not break the linker script sort). Run tests/tst-mremap.so and
  confirm the new EINVAL cases (old_size==0, MAP_PRIVATE writable move) pass.

PR#1433  libaio: implement Linux async I/O           branch pr/libaio  head 41d79a2
  Changes: <unistd.h> include; io_setup nr_events<=0 EINVAL / null ctxp EFAULT;
  io_submit null-ios/null-iocb EFAULT + thread-make-null handling; io_getevents
  null-events EFAULT + invalid-timespec EINVAL; io_destroy now waits on a new
  ctx->waiters count (UAF fix vs concurrent io_getevents).
  VALIDATE: compile. Run tests/tst-libaio.so (existing cases still pass with the
  new EINVAL/EFAULT guards). CONCURRENCY: ideally add+run a stress case that calls
  io_destroy while another thread is parked in io_getevents to exercise the
  waiters handshake (no UAF, no deadlock). Also the io_setup(0) path is now EINVAL
  (test uses 8/16/4 so unaffected).

PR#1435  libc: setrlimit/getrlimit consistent        branch pr/setrlimit head 83bd225
  Changes: test now defines _GNU_SOURCE before <sys/resource.h> (prlimit decl);
  null rlim pointer now EFAULT (was EINVAL) in get/setrlimit; added null-ptr test
  asserts.
  VALIDATE: compile tests/tst-rlimit.so (this was the exact build blocker Copilot
  flagged) and run it; confirm the new EFAULT null-pointer asserts pass.

PR#1436  fs: implement close_range(2)                branch pr/close-range head 7c95715
  Changes: test hardening only (capture fcntl return, assert !=-1, then check
  FD_CLOEXEC bit).
  VALIDATE: compile+run tests/tst-close-range.so.

PR#1450  drivers: validate device completion ids     branch pr/sec-driver-backend head 68078e5
  Changes: virtio get_buf_gc() bounds-checks elem._id + _desc[]._next chain;
  get_buf_elem() now advances past a bogus id instead of returning nullptr (was a
  queue-stall DoS); NVMe completion path also null-checks lazily-allocated
  _pending_bios[row].
  VALIDATE: compile. This is a hot storage path -- run real block I/O across
  virtio-blk and NVMe (heavy fio/ZFS/ext4 workload) to confirm no regression in
  normal completion draining (the get_buf_elem loop restructure is the risk).
  1-CPU and N-CPU. Malicious-id path is hard to trigger without a crafted backend;
  at minimum confirm normal traffic still completes.

PR#1451  fs: validate ROFS on-disk metadata          branch pr/sec-rofs head 737322e
  Changes: in_bounds() upper-bound guard; uint64_t loop counters; calloc + single
  fail() cleanup path for mount-failure leaks; readdir/lookup uint64_t-wrap guard;
  (pre-existing) read_link PATH_MAX-1 off-by-one fix.
  VALIDATE: compile. Boot a normal ROFS image and confirm mount + directory
  listing + symlink resolution still work (the calloc/cleanup refactor and the
  uint64_t loop-counter change are the risk). If a crafted-image harness exists,
  run it to confirm the bad-metadata paths return EINVAL/ENOMEM cleanly rather
  than corrupting memory.

PR#1431  libext: real fsync + page-cache bridge       branch pr/ext4-fsync-cache head a17fcf7
  Changes this sweep: tests/tst-ext4-rw.cc now SKIPs (returns 0 with a message)
  when /data/readme.dat is absent, instead of asserting -- this test is in the
  default tests:= list and was aborting default runs without an ext4 second disk.
  (The page-leak/void->bool fix and the "ponytail" removal were already in the tip
  from a prior push.)
  VALIDATE: (a) default test run WITHOUT an ext4 disk -> tst-ext4-rw must SKIP and
  the suite must not fail. (b) WITH the ext4 second disk mounted at /data
  containing readme.dat (12288 bytes, pattern (i*7+3)&0xff) -> the mmap/vop_cache
  and fsync assertions must pass.

NOTE (all branches): each was rebased onto upstream/master (cb8c720) and its
two-dot diff verified feature-only (0 osv_sigtimedwait reverts, no tst-signal-fills
deletion), so no stale-base silent-revert risk on these pushes.
