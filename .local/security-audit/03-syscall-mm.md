# Audit Agent 3: syscall/mm (8746e5e4) - COMPLETE
## CONFIRMED
1. HIGH DoS (CVSS~7.1): unbounded iovcnt -> kernel abort + stack VLA + pre-assert OOB walk. vfs_syscalls.cc:271-272,310-311: assert(niov<=UIO_MAXIOV) stays live in _KERNEL builds -> abort()=VM panic. Unvalidated from preadv/pwritev/readv/writev, preadv2/pwritev2 (fssc), io_uring READV/WRITEV/_FIXED (iovcnt=sqe->len u32), libaio PREADV/PWRITEV (aio_nbytes u64). PoC: readv(fd, v, 0x40000000) -> panic. Also VLA copy_iov[niov] on kernel stack + summation loop derefs iov[i] up to niov before assert. FIX: EINVAL (not assert) on niov>UIO_MAXIOV at top of sys_read/write + clamp at callers; drop VLA.
2. MED-HIGH DoS (CVSS~6.5): ZFS read-cache page LEAK. core/pagecache.cc:185 ~cached_page(){} empty, doesn't free _page. Correct for ROFS (borrowed) but ZFS zfs_vop_cache always osv_alloc_page()s an OWNED page -> dropped at :299/:341 leaks 4KB. Shared cached_page type, no borrowed/owned tag. Trigger: mmap ZFS file, read-fault then write-fault, repeat over large file -> DRAM exhaustion. (No double-free on THIS branch; ARC-sharing subclass only in openzfs-audit worktree.) FIX: tag ownership or ZFS cached_page subclass freeing _page.
3. MED (CVSS~6.4): mremap grow-in-place TOCTOU. mmu.cc:1453 range_is_free under read-lock, released, then :1494-1499 MAP_FIXED evacuate destroys any VMA now in [end,end+grow) -> UAF for racing mmap. FIX: hold vma_list_mutex across check+fixed insert. [converges Agent1 #4]
## SUSPECTED (ELF loader - only if app dlopens attacker-controlled .so: plugins/JVM native libs)
4. MED: elf.cc:330 file::read integer overflow: offset+size wraps, passes "too short" check -> OOB file read. FIX: check without addition.
5. MED-HIGH: elf.cc:769/844 relocation r_offset not bounds-checked vs PT_LOAD segments; sym indices unchecked -> arbitrary kernel write during load of crafted .so. FIX: validate r_offset in [_base,_end) + sym idx vs table.
## LOW: mmap(length=~0) -> align_up wraps to 0-size VMA (functional, not corruption).
## CLEAN: io_uring SQE fetch (array_idx bounds-checked, SQE snapshotted, MAX_ENTRIES cap, READ/WRITE_FIXED range ok), close_range, setrlimit, signalfd/inotify read bounds, splice/vmsplice/tee, getdents64, sys_read/write total-len overflow guarded, get_mempolicy nmask, sched_getaffinity.
## TOP: #1 (any app panics kernel via readv) + #2 (ZFS OOM under normal mmap+write).
